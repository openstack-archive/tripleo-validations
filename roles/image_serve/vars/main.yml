---
metadata:
  name: Image-serve availability
  description: Verify that image-serve service is ready
  groups:
    - pre-upgrade
    - pre-update
    - post-deployment
    - post-upgrade
    - post-update
