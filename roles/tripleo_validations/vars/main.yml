---
validations_list:
  compute:
    - nova-status
  network:
    - undercloud-neutron-sanity-check
  validation:
    - check-cpu
    - 512e
  tripleo:
    - undercloud-process-count
validations_group:
  compute:
    - compute
  network:
    - network
