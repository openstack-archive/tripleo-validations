---
- hosts: all
  vars:
    metadata:
      name: System encoding
      description: |
        Ensure the local is unicode
      groups:
        - pre-deployment
        - pre-upgrade
        - pre-update
      categories:
        - os
        - system
        - encoding
      products:
        - tripleo
    system_encoding_debug: false
  roles:
    - system_encoding
